- name: install packages
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: loop through packages
    ansible.builtin.package:
      name: "{{ item }}"
      state: latest
      
    loop:
     - nginx
     - mysql
     - redis